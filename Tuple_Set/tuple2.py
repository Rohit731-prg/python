# find the maximum and ninimum value in a tuple

tp = (10, 20, 30, 40, 50, 5, 35)
max_value = max(tp)
min_value = min(tp)

print("Maximum value in the tuple is:", max_value)
print("Minimum value in the tuple is:", min_value)